filebeat.inputs:
  - type: log
    paths:
      - "/csvtoparse/*.csv"
#      - "/csvtoparse/*.csv"
#      - "/csvtoparse/*.csv"
    # fields:
    #   csv_file: prueba
    # fieldas_under_root: true

output.logstash:
    hosts: ["logstash:5044"]
